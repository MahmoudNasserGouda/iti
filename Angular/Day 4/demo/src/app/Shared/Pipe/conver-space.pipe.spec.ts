import { ConverSpacePipe } from './conver-space.pipe';

describe('ConverSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new ConverSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
