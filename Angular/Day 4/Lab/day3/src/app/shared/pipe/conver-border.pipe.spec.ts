import { ConverBorderPipe } from './conver-border.pipe';

describe('ConverBorderPipe', () => {
  it('create an instance', () => {
    const pipe = new ConverBorderPipe();
    expect(pipe).toBeTruthy();
  });
});
